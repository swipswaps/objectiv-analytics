// TODO temporary, we need a better architecture for these
export const windowAvailable = typeof window !== 'undefined';
export const documentAvailable = typeof window.document !== 'undefined';
export const navigatorAvailable = typeof window.navigator !== 'undefined';
