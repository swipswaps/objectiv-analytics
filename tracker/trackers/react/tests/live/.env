# Disable CRA's preflight check as it detects our monorepo node_modules higher up and prevents the App from starting
SKIP_PREFLIGHT_CHECK=true

# Custom port to not collide with ROD (3000) and Collector (5000)
PORT=4000
