{
  "name": "@objectiv/schema",
  "version": "0.0.1",
  "description": "Objectiv Schema TypeScript",
  "types": "src/index.d.ts",
  "files": [
    "src"
  ],
  "scripts": {
    "prettier": "prettier --write .",
    "generate": "(cd src && node generator.js) && yarn prettier && yarn tsc --noEmit"
  },
  "devDependencies": {
    "prettier": "^2.3.0",
    "typescript": "^4.2.4"
  }
}
